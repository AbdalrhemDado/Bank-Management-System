public class TransactionProcessor {
    public void executeDeposit(IAccount account, double amount) {
        account.deposit(amount);
    }

    public void executeWithdraw(IAccount account, double amount) {
        account.withdraw(amount);
    }
}
